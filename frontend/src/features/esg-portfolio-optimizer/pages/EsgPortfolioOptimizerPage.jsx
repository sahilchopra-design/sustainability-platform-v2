import React,{useState,useMemo} from 'react';
import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid,Legend,Cell,LineChart,Line,PieChart,Pie,RadarChart,Radar,PolarGrid,PolarAngleAxis,PolarRadiusAxis,AreaChart,Area,ScatterChart,Scatter,ZAxis} from 'recharts';
const T={bg:'#f6f4f0',surface:'#ffffff',surfaceH:'#f0ede7',border:'#e5e0d8',borderL:'#d5cfc5',navy:'#1b3a5c',navyL:'#2c5a8c',gold:'#c5a96a',goldL:'#d4be8a',sage:'#5a8a6a',sageL:'#7ba67d',teal:'#5a8a6a',text:'#1b3a5c',textSec:'#5c6b7e',textMut:'#9aa3ae',red:'#dc2626',green:'#16a34a',amber:'#d97706',font:"'DM Sans','SF Pro Display',system-ui,-apple-system,sans-serif",mono:"'JetBrains Mono','SF Mono','Fira Code',monospace"};
const sr=(s)=>{let x=Math.sin(s+1)*10000;return x-Math.floor(x);};const fmt1=n=>Number(n).toFixed(1);const fmt0=n=>Number(n).toFixed(0);const fmt2=n=>Number(n).toFixed(2);const scoreColor=v=>v>=70?T.green:v>=45?T.amber:T.red;const riskColor=v=>v==='Low'?T.green:v==='Medium'?T.amber:v==='High'?T.red:'#7c3aed';const COLORS=[T.navy,T.gold,T.sage,T.navyL,T.goldL,T.sageL,T.red,T.amber,T.green,'#7c3aed'];
const CONSTRAINTS=['Min ESG 60','Max Carbon 200','Paris Aligned','Net Zero','Exclusion List','Best-in-Class','SDG Aligned','SFDR Art 8'];
const SECTORS=['Technology','Healthcare','Financials','Energy','Industrials','Materials','Consumer','Utilities','Real Estate','Telecom'];
const RISK_LEVELS=['Low','Medium','High','Critical'];
const NAMES=['Global ESG Optimal','Low Carbon Efficient','Paris Aligned Core','Transition Portfolio','Green Balanced Fund','Impact Optimized','Clean Energy Tilt','Sustainable Core','ESG Momentum Blend','Quality Green','Climate Smart Mix','Water Risk Adjusted','Circular Efficient','SDG Aligned Core','SFDR Art 9 Optimal','Net Zero Balanced','Social Impact Tilt','Governance First','Diversity Optimized','Stewardship Core','Just Transition Mix','Nature Positive Opt','Biodiversity Smart','Ocean Risk Balanced','Air Quality Tilt','Land Use Adjusted','Supply Chain ESG','Human Rights Screen','Anti-Corruption Opt','Health Safety Core','Living Wage Tilt','Modern Slavery Free','Community Impact','Stakeholder Value','Double Materiality','Taxonomy Aligned','CSRD Ready','Climate Overlay','Factor ESG Blend','Multi-Asset Green','Real Asset Climate','Infra ESG Balanced','Private Market Opt','EM Sustainable','Frontier ESG','Small Cap Green','Mid Cap Balanced','Large Cap Climate','Global Core ESG','Regional Optimized'];
const cardS={background:T.surface,border:`1px solid ${T.border}`,borderRadius:8,padding:20,marginBottom:16};const inputS={fontFamily:T.mono,fontSize:13,padding:'8px 14px',border:`1px solid ${T.border}`,borderRadius:6,outline:'none',background:T.surface,color:T.text,width:260};const btnS=(a)=>({fontFamily:T.font,fontSize:13,fontWeight:a?700:500,padding:'8px 18px',border:`1px solid ${a?T.gold:T.border}`,borderRadius:6,background:a?T.gold:T.surface,color:a?'#fff':T.text,cursor:'pointer'});const thS={fontFamily:T.mono,fontSize:12,fontWeight:600,color:T.textSec,padding:'10px 12px',textAlign:'left',borderBottom:`2px solid ${T.border}`,cursor:'pointer',userSelect:'none',whiteSpace:'nowrap'};const tdS={fontFamily:T.font,fontSize:13,color:T.text,padding:'10px 12px',borderBottom:`1px solid ${T.borderL}`};const badgeS=(bg)=>({display:'inline-block',padding:'2px 10px',borderRadius:99,fontSize:11,fontWeight:600,fontFamily:T.mono,background:bg+'18',color:bg});const kpiBoxS={background:T.surface,border:`1px solid ${T.border}`,borderRadius:8,padding:16,textAlign:'center',flex:1,minWidth:140};const kpiVal={fontFamily:T.mono,fontSize:26,fontWeight:700,color:T.navy};const kpiLab={fontFamily:T.font,fontSize:11,color:T.textMut,marginTop:4,textTransform:'uppercase',letterSpacing:0.5};
const exportCSV=(rows,fn)=>{if(!rows.length)return;const ks=Object.keys(rows[0]);const csv=[ks.join(','),...rows.map(r=>ks.map(k=>JSON.stringify(r[k]??'')).join(','))].join('\n');const b=new Blob([csv],{type:'text/csv'});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=fn;a.click();URL.revokeObjectURL(u);};
const CT=({active,payload,label})=>{if(!active||!payload?.length)return null;return(<div style={{background:T.surface,border:`1px solid ${T.border}`,borderRadius:6,padding:'10px 14px',fontFamily:T.font,fontSize:12}}><div style={{fontWeight:700,color:T.navy,marginBottom:4}}>{label}</div>{payload.map((p,i)=><div key={i} style={{color:p.color||T.text}}>{p.name}: {typeof p.value==='number'?fmt2(p.value):p.value}</div>)}</div>);};
const genData=(n)=>{const d=[];for(let i=0;i<n;i++){const s=idx=>sr(i*idx+idx);d.push({id:i+1,name:NAMES[i%NAMES.length]+(i>=NAMES.length?` ${Math.floor(i/NAMES.length)+1}`:''),constraint:CONSTRAINTS[Math.floor(s(17)*CONSTRAINTS.length)],sector:SECTORS[Math.floor(s(23)*SECTORS.length)],risk:RISK_LEVELS[Math.floor(s(29)*RISK_LEVELS.length)],
  esgScore:Math.floor(25+s(31)*70),weight:Number((0.5+s(37)*4.5).toFixed(2)),optWeight:Number((0.3+s(41)*4.7).toFixed(2)),expReturn:Number((2+s(43)*14).toFixed(2)),
  vol:Number((5+s(47)*20).toFixed(1)),sharpe:Number((0.2+s(53)*2.3).toFixed(2)),trackingError:Number((0.3+s(59)*5.7).toFixed(2)),
  carbonInt:Math.floor(10+s(61)*490),optCarbonInt:Math.floor(5+s(67)*300),tempAlign:Number((1.2+s(71)*2.3).toFixed(1)),
  envScore:Math.floor(20+s(73)*75),socScore:Math.floor(20+s(79)*75),govScore:Math.floor(20+s(83)*75),
  maxDD:Number((-2-s(89)*25).toFixed(1)),beta:Number((0.3+s(97)*1.4).toFixed(2)),alpha:Number((-1+s(101)*5).toFixed(2)),
  q1:Number((0.5+s(103)*2).toFixed(2)),q2:Number((0.6+s(107)*1.9).toFixed(2)),q3:Number((0.7+s(109)*1.8).toFixed(2)),q4:Number((0.55+s(113)*1.95).toFixed(2)),
  greenRev:Math.floor(s(127)*65),sbtiAligned:s(131)>0.5?'Yes':'No',exclusions:Math.floor(s(137)*20),turnover:Math.floor(5+s(139)*95),});}return d;};
const DATA=genData(50);const TABS=['Optimization Overview','Constraint Analysis','ESG Attribution','Frontier Analysis'];const PAGE_SIZE=12;
export default function EsgPortfolioOptimizerPage(){
  const[tab,setTab]=useState(0);const[search,setSearch]=useState('');const[sortCol,setSortCol]=useState('sharpe');const[sortDir,setSortDir]=useState('desc');const[page,setPage]=useState(0);const[expanded,setExpanded]=useState(null);const[fCon,setFCon]=useState('All');const[fSector,setFSector]=useState('All');
  const filtered=useMemo(()=>{let d=[...DATA];if(search){const s=search.toLowerCase();d=d.filter(r=>r.name.toLowerCase().includes(s)||r.sector.toLowerCase().includes(s)||r.constraint.toLowerCase().includes(s));}if(fCon!=='All')d=d.filter(r=>r.constraint===fCon);if(fSector!=='All')d=d.filter(r=>r.sector===fSector);d.sort((a,b)=>{const av=a[sortCol],bv=b[sortCol];if(av==null)return 1;if(bv==null)return -1;return sortDir==='asc'?(av>bv?1:-1):(av<bv?1:-1);});return d;},[search,sortCol,sortDir,fCon,fSector]);
  const paged=useMemo(()=>filtered.slice(page*PAGE_SIZE,(page+1)*PAGE_SIZE),[filtered,page]);const totalPages=Math.ceil(filtered.length/PAGE_SIZE);
  const toggleSort=(c)=>{if(sortCol===c)setSortDir(d=>d==='asc'?'desc':'asc');else{setSortCol(c);setSortDir('desc');}setPage(0);};const sortArrow=(c)=>sortCol===c?(sortDir==='asc'?' \u25B2':' \u25BC'):'';
  const kpis=useMemo(()=>{const d=filtered;if(!d.length)return{count:0,avgSharpe:0,avgEsg:0,avgReturn:0,avgVol:0,avgCarbon:0};return{count:d.length,avgSharpe:d.reduce((a,r)=>a+r.sharpe,0)/ Math.max(1, d.length),avgEsg:d.reduce((a,r)=>a+r.esgScore,0)/ Math.max(1, d.length),avgReturn:d.reduce((a,r)=>a+r.expReturn,0)/ Math.max(1, d.length),avgVol:d.reduce((a,r)=>a+r.vol,0)/ Math.max(1, d.length),avgCarbon:d.reduce((a,r)=>a+r.carbonInt,0)/ Math.max(1, d.length)};},[filtered]);
  const conDist=useMemo(()=>{const m={};filtered.forEach(r=>{m[r.constraint]=(m[r.constraint]||0)+1;});return Object.entries(m).map(([name,value])=>({name:name.length>14?name.slice(0,14)+'..':name,value})).sort((a,b)=>b.value-a.value);},[filtered]);
  const sectorAlloc=useMemo(()=>SECTORS.map(s=>({name:s.length>10?s.slice(0,10)+'..':s,current:filtered.filter(r=>r.sector===s).reduce((a,r)=>a+r.weight,0),opt:filtered.filter(r=>r.sector===s).reduce((a,r)=>a+r.optWeight,0)})).filter(d=>d.current>0||d.opt>0),[filtered]);
  const radarData=useMemo(()=>{if(!filtered.length)return[];const avg=k=>filtered.reduce((a,r)=>a+r[k],0)/ Math.max(1, filtered.length);return[{axis:'ESG',value:avg('esgScore')},{axis:'Env',value:avg('envScore')},{axis:'Soc',value:avg('socScore')},{axis:'Gov',value:avg('govScore')},{axis:'Green Rev',value:avg('greenRev')},{axis:'Carbon Eff',value:100-avg('carbonInt')/5}];},[filtered]);
  const trendData=useMemo(()=>['Q1','Q2','Q3','Q4'].map((q,i)=>({quarter:q,sharpe:filtered.reduce((a,r)=>a+[r.q1,r.q2,r.q3,r.q4][i],0)/(filtered.length||1)})),[filtered]);
  const renderKPIs=()=>(<div style={{display:'flex',gap:12,flexWrap:'wrap',marginBottom:16}}><div style={kpiBoxS}><div style={kpiVal}>{kpis.count}</div><div style={kpiLab}>Portfolios</div></div><div style={kpiBoxS}><div style={{...kpiVal,color:kpis.avgSharpe>=1?T.green:T.amber}}>{fmt2(kpis.avgSharpe)}</div><div style={kpiLab}>Avg Sharpe</div></div><div style={kpiBoxS}><div style={{...kpiVal,color:scoreColor(kpis.avgEsg)}}>{fmt1(kpis.avgEsg)}</div><div style={kpiLab}>Avg ESG</div></div><div style={kpiBoxS}><div style={kpiVal}>{fmt1(kpis.avgReturn)}%</div><div style={kpiLab}>Avg Return</div></div><div style={kpiBoxS}><div style={kpiVal}>{fmt1(kpis.avgVol)}%</div><div style={kpiLab}>Avg Vol</div></div><div style={kpiBoxS}><div style={kpiVal}>{fmt0(kpis.avgCarbon)}</div><div style={kpiLab}>Avg Carbon Int</div></div></div>);
  const renderFilters=()=>(<div style={{display:'flex',gap:10,flexWrap:'wrap',marginBottom:16,alignItems:'center'}}><input style={inputS} placeholder="Search portfolios..." value={search} onChange={e=>{setSearch(e.target.value);setPage(0);}}/><select style={{...inputS,width:160}} value={fCon} onChange={e=>{setFCon(e.target.value);setPage(0);}}><option value="All">All Constraints</option>{CONSTRAINTS.map(c=><option key={c}>{c}</option>)}</select><select style={{...inputS,width:150}} value={fSector} onChange={e=>{setFSector(e.target.value);setPage(0);}}><option value="All">All Sectors</option>{SECTORS.map(s=><option key={s}>{s}</option>)}</select><button style={{...btnS(false),marginLeft:'auto'}} onClick={()=>exportCSV(filtered,'esg_optimizer.csv')}>Export CSV</button></div>);
  const renderPagination=()=>(<div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:12,fontFamily:T.mono,fontSize:12,color:T.textSec}}><span>{page*PAGE_SIZE+1}-{Math.min((page+1)*PAGE_SIZE,filtered.length)} of {filtered.length}</span><div style={{display:'flex',gap:4}}><button style={btnS(false)} disabled={page===0} onClick={()=>setPage(p=>p-1)}>Prev</button>{Array.from({length:Math.min(totalPages,7)},(_,i)=>{let pg=i;if(totalPages>7){if(page<4)pg=i;else if(page>=totalPages-3)pg=totalPages-7+i;else pg=page-3+i;}return <button key={pg} style={btnS(pg===page)} onClick={()=>setPage(pg)}>{pg+1}</button>;})}<button style={btnS(false)} disabled={page>=totalPages-1} onClick={()=>setPage(p=>p+1)}>Next</button></div></div>);
  const renderExpanded=(r)=>(<tr key={`exp-${r.id}`}><td colSpan={99} style={{background:T.surfaceH,padding:20,borderBottom:`1px solid ${T.border}`}}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr',gap:16}}><div><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:4}}>ALLOCATION</div><div style={{fontSize:13,lineHeight:1.8}}><div><strong>Constraint:</strong> {r.constraint}</div><div><strong>Sector:</strong> {r.sector}</div><div><strong>Weight:</strong> {r.weight}%</div><div><strong>Opt Weight:</strong> <span style={{color:T.green}}>{r.optWeight}%</span></div><div><strong>Turnover:</strong> {r.turnover}%</div><div><strong>Exclusions:</strong> {r.exclusions}</div></div></div><div><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:4}}>PERFORMANCE</div><div style={{fontSize:13,lineHeight:1.8}}><div><strong>Exp Return:</strong> {r.expReturn}%</div><div><strong>Vol:</strong> {r.vol}%</div><div><strong>Sharpe:</strong> <span style={{fontWeight:700,color:r.sharpe>=1?T.green:T.amber}}>{r.sharpe}</span></div><div><strong>Alpha:</strong> <span style={{color:r.alpha>=0?T.green:T.red}}>{r.alpha}%</span></div><div><strong>Beta:</strong> {r.beta}</div><div><strong>Max DD:</strong> <span style={{color:T.red}}>{r.maxDD}%</span></div><div><strong>TE:</strong> {r.trackingError}%</div></div></div><div><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:4}}>ESG METRICS</div><div style={{fontSize:13,lineHeight:1.8}}><div><strong>ESG:</strong> <span style={{color:scoreColor(r.esgScore),fontWeight:700}}>{r.esgScore}</span></div><div><strong>Env:</strong> {r.envScore}</div><div><strong>Soc:</strong> {r.socScore}</div><div><strong>Gov:</strong> {r.govScore}</div><div><strong>Carbon:</strong> {r.carbonInt} / Opt: {r.optCarbonInt}</div><div><strong>Temp:</strong> {r.tempAlign}C</div><div><strong>Green Rev:</strong> {r.greenRev}%</div><div><strong>SBTi:</strong> <span style={{color:r.sbtiAligned==='Yes'?T.green:T.red}}>{r.sbtiAligned}</span></div></div></div></div></td></tr>);
  const renderTable=(cols)=>(<div style={{...cardS,overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr>{cols.map(([k,l])=><th key={k} style={thS} onClick={()=>toggleSort(k)}>{l}{sortArrow(k)}</th>)}</tr></thead><tbody>{paged.map(r=>(<React.Fragment key={r.id}><tr style={{cursor:'pointer',background:expanded===r.id?T.surfaceH:'transparent'}} onClick={()=>setExpanded(expanded===r.id?null:r.id)}>{cols.map(([k])=>{const v=r[k];if(k==='name')return <td key={k} style={{...tdS,fontWeight:600,color:T.navy}}>{v}</td>;if(k==='risk')return <td key={k} style={tdS}><span style={badgeS(riskColor(v))}>{v}</span></td>;if(k==='esgScore')return <td key={k} style={{...tdS,fontWeight:700,color:scoreColor(v)}}>{v}</td>;if(k==='alpha')return <td key={k} style={{...tdS,fontFamily:T.mono,color:v>=0?T.green:T.red}}>{v>0?'+':''}{v}</td>;if(typeof v==='number')return <td key={k} style={{...tdS,fontFamily:T.mono}}>{Math.abs(v)<10?fmt2(v):fmt1(v)}</td>;return <td key={k} style={tdS}>{v}</td>;})}</tr>{expanded===r.id&&renderExpanded(r)}</React.Fragment>))}</tbody></table>{renderPagination()}</div>);
  const renderTab0=()=>(<>{renderKPIs()}{renderFilters()}<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16,marginBottom:16}}><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>CONSTRAINT DISTRIBUTION</div><ResponsiveContainer width="100%" height={260}><PieChart><Pie data={conDist} cx="50%" cy="50%" outerRadius={90} innerRadius={45} dataKey="value" label={({name,percent})=>`${name} ${(percent*100).toFixed(0)}%`}>{conDist.map((e,i)=><Cell key={i} fill={COLORS[i%COLORS.length]}/>)}</Pie><Tooltip/><Legend/></PieChart></ResponsiveContainer></div><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>CURRENT vs OPTIMIZED ALLOCATION</div><ResponsiveContainer width="100%" height={260}><BarChart data={sectorAlloc}><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="name" tick={{fontSize:10}} angle={-20} textAnchor="end" height={50}/><YAxis tick={{fontSize:11}}/><Tooltip content={<CT/>}/><Legend/><Bar dataKey="current" name="Current" fill={T.red} radius={[2,2,0,0]}/><Bar dataKey="opt" name="Optimized" fill={T.green} radius={[2,2,0,0]}/></BarChart></ResponsiveContainer></div></div><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>EFFICIENT FRONTIER: RETURN vs VOL</div><ResponsiveContainer width="100%" height={300}><ScatterChart><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="x" name="Vol%" tick={{fontSize:11}}/><YAxis dataKey="y" name="Return%" tick={{fontSize:11}}/><Tooltip content={({active,payload})=>{if(!active||!payload?.length)return null;const d=payload[0].payload;return <div style={{background:T.surface,border:`1px solid ${T.border}`,borderRadius:6,padding:10,fontSize:12}}><div style={{fontWeight:700}}>{d.n}</div><div>Vol:{fmt1(d.x)}% Ret:{fmt2(d.y)}%</div></div>}}/><Scatter data={filtered.map(r=>({n:r.name,x:r.vol,y:r.expReturn}))} fill={T.navy}/></ScatterChart></ResponsiveContainer></div>{renderTable([['name','Portfolio'],['constraint','Constraint'],['sharpe','Sharpe'],['expReturn','Return%'],['vol','Vol%'],['esgScore','ESG'],['carbonInt','Carbon'],['risk','Risk']])}</>);
  const renderTab1=()=>(<>{renderFilters()}<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16,marginBottom:16}}><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>ESG RADAR</div><ResponsiveContainer width="100%" height={300}><RadarChart data={radarData}><PolarGrid stroke={T.borderL}/><PolarAngleAxis dataKey="axis" tick={{fontSize:11}}/><PolarRadiusAxis domain={[0,100]} tick={{fontSize:10}}/><Radar name="Avg" dataKey="value" stroke={T.navy} fill={T.navy} fillOpacity={0.2}/><Tooltip/></RadarChart></ResponsiveContainer></div><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>CARBON: CURRENT vs OPTIMIZED</div><ResponsiveContainer width="100%" height={300}><BarChart data={CONSTRAINTS.map(c=>{const items=filtered.filter(r=>r.constraint===c);if(!items.length)return null;return{name:c.length>14?c.slice(0,14)+'..':c,current:items.reduce((a,r)=>a+r.carbonInt,0)/ Math.max(1, items.length),opt:items.reduce((a,r)=>a+r.optCarbonInt,0)/ Math.max(1, items.length)};}).filter(Boolean)}><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="name" tick={{fontSize:9}} angle={-25} textAnchor="end" height={55}/><YAxis tick={{fontSize:11}}/><Tooltip content={<CT/>}/><Legend/><Bar dataKey="current" name="Current" fill={T.red} radius={[2,2,0,0]}/><Bar dataKey="opt" name="Optimized" fill={T.green} radius={[2,2,0,0]}/></BarChart></ResponsiveContainer></div></div>{renderTable([['name','Portfolio'],['constraint','Constraint'],['carbonInt','Carbon'],['optCarbonInt','Opt Carbon'],['tempAlign','Temp C'],['trackingError','TE%'],['exclusions','Excl'],['risk','Risk']])}</>);
  const renderTab2=()=>(<>{renderFilters()}<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16,marginBottom:16}}><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>E/S/G BY CONSTRAINT</div><ResponsiveContainer width="100%" height={280}><BarChart data={CONSTRAINTS.map(c=>{const items=filtered.filter(r=>r.constraint===c);if(!items.length)return null;return{name:c.length>14?c.slice(0,14)+'..':c,env:items.reduce((a,r)=>a+r.envScore,0)/ Math.max(1, items.length),soc:items.reduce((a,r)=>a+r.socScore,0)/ Math.max(1, items.length),gov:items.reduce((a,r)=>a+r.govScore,0)/ Math.max(1, items.length)};}).filter(Boolean)}><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="name" tick={{fontSize:9}} angle={-25} textAnchor="end" height={55}/><YAxis domain={[0,100]} tick={{fontSize:11}}/><Tooltip content={<CT/>}/><Legend/><Bar dataKey="env" name="Env" fill={T.sage}/><Bar dataKey="soc" name="Soc" fill={T.gold}/><Bar dataKey="gov" name="Gov" fill={T.navy}/></BarChart></ResponsiveContainer></div><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>GREEN REVENUE DISTRIBUTION</div><ResponsiveContainer width="100%" height={280}><AreaChart data={[...filtered].sort((a,b)=>a.greenRev-b.greenRev).map((r,i)=>({rank:i+1,green:r.greenRev}))}><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="rank" tick={{fontSize:11}}/><YAxis tick={{fontSize:11}}/><Tooltip content={<CT/>}/><Area type="monotone" dataKey="green" stroke={T.sage} fill={T.sage} fillOpacity={0.15} name="Green Rev%"/></AreaChart></ResponsiveContainer></div></div>{renderTable([['name','Portfolio'],['esgScore','ESG'],['envScore','Env'],['socScore','Soc'],['govScore','Gov'],['greenRev','Green%'],['sbtiAligned','SBTi'],['turnover','Turn%']])}</>);
  const renderTab3=()=>(<>{renderFilters()}<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:16,marginBottom:16}}><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>SHARPE TREND</div><ResponsiveContainer width="100%" height={280}><LineChart data={trendData}><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="quarter" tick={{fontSize:12}}/><YAxis tick={{fontSize:11}}/><Tooltip content={<CT/>}/><Line type="monotone" dataKey="sharpe" stroke={T.navy} strokeWidth={2} dot={{r:4}} name="Avg Sharpe"/></LineChart></ResponsiveContainer></div><div style={cardS}><div style={{fontFamily:T.mono,fontSize:11,color:T.textMut,marginBottom:8}}>ALPHA vs MAX DRAWDOWN</div><ResponsiveContainer width="100%" height={280}><ScatterChart><CartesianGrid strokeDasharray="3 3" stroke={T.borderL}/><XAxis dataKey="x" name="Alpha%" tick={{fontSize:11}}/><YAxis dataKey="y" name="Max DD%" tick={{fontSize:11}}/><Tooltip content={({active,payload})=>{if(!active||!payload?.length)return null;const d=payload[0].payload;return <div style={{background:T.surface,border:`1px solid ${T.border}`,borderRadius:6,padding:10,fontSize:12}}><div style={{fontWeight:700}}>{d.n}</div><div>Alpha:{fmt2(d.x)}% DD:{fmt1(d.y)}%</div></div>}}/><Scatter data={filtered.map(r=>({n:r.name,x:r.alpha,y:r.maxDD}))} fill={T.red}/></ScatterChart></ResponsiveContainer></div></div>{renderTable([['name','Portfolio'],['q1','Q1'],['q2','Q2'],['q3','Q3'],['q4','Q4'],['alpha','Alpha'],['maxDD','Max DD'],['beta','Beta']])}</>);
  return(<div style={{fontFamily:T.font,background:T.bg,minHeight:'100vh',padding:24}}><div style={{maxWidth:1400,margin:'0 auto'}}><div style={{marginBottom:20}}><h1 style={{fontFamily:T.font,fontSize:22,fontWeight:700,color:T.navy,margin:0}}>ESG Portfolio Optimizer</h1><p style={{fontFamily:T.font,fontSize:13,color:T.textSec,marginTop:2}}>ESG-constrained portfolio optimization across {DATA.length} portfolios</p></div><div style={{display:'flex',gap:6,marginBottom:20,borderBottom:`2px solid ${T.border}`,paddingBottom:8}}>{TABS.map((t,i)=><button key={i} style={btnS(tab===i)} onClick={()=>{setTab(i);setPage(0);setExpanded(null);}}>{t}</button>)}</div>{tab===0&&renderTab0()}{tab===1&&renderTab1()}{tab===2&&renderTab2()}{tab===3&&renderTab3()}</div></div>);
}
